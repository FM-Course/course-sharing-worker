{
  "name": "course-sharing-download-worker",
  "version": "2.0.0",
  "description": "Cloudflare Worker for downloading course files from R2 storage",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --persist-to ../.wrangler/state --config wrangler-dev.toml",
    "deploy": "wrangler deploy",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "cloudflare-worker",
    "r2",
    "download",
    "course-sharing"
  ],
  "author": "Course Sharing Team",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "4.76.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
